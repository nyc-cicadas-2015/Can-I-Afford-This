<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div data-alert class="alert-box <%= name.to_s == 'notice' ? 'message' : 'error' %>">
    <strong><%= content_tag :div, msg %></strong>
    <a href="#" class="close">&times;</a>
    </div>
  <% end %>
<% end %>

<%= form_for :session, url: login_path, html: {id: "login_form"} do |f| %>
  <%= f.text_field :email, placeholder: "Email" %>
  <%= f.password_field :password, placeholder: "Password" %>
  <br>
  <%= f.submit "Login", :class => "button round" %>
<% end %>
